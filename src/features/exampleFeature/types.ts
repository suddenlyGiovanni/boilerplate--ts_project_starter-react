/**
 * @description Types
 * Let's start from defining the constants we will use as redux action types.
 * In order to keep the naming simple, let's call the file`types.ts`,
 * because`constants.js` is a bit too generic.
 * Our examples will model a real duck.
 */

export const QUACK: 'duck/QUACK' = 'duck/QUACK'
export const SWIM: 'duck/SWIM' = 'duck/SWIM'
